  Usage:
      $0 COMMAND [options]

  Commands:

      help
                          print usage information

      -v, --version
                          show version info and exit

      --init
                          initialize a new repo

      -a, --adfile <file> [file2] [file3] ...
                          makes the listed files available to commit

      --git <git command>
                          pass command through to git (within ssdm context)

  Options:
